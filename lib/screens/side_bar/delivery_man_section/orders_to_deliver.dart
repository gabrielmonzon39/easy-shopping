import 'package:flutter/cupertino.dart';

class OrdersToDeliver extends StatelessWidget {
  const OrdersToDeliver({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 0,
    );
  }
}
